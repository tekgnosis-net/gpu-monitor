"""Entry point for the gpu_monitor package.

Run via `python3 -m gpu_monitor`. Replaces `./monitor_gpu.sh` as the
container CMD.

Filled in during Step 6 of the refactor.
"""

if __name__ == "__main__":
    raise SystemExit(
        "gpu_monitor entry point not yet implemented "
        "(see /home/kumar/.claude/plans/steady-rolling-thompson.md, Step 6)."
    )
